-- filter the group by -- apply filter to a group as a whole
-- aggregate func are not allowded in where => count(e.emp_no)
-- but we can use where if it is not used in aggregate func.4
-- having ma filter garna lai ki aggre func ma use garna paryo or group by ma use 
-- garna paryoo.

-- having applies in groups , where applies in individual records